{
  "name": "schmitt-odyssee",
  "version": "1.0.0",
  "description": "Schmitt Odyssée - Jeu de plateau interactif multiplateforme",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:mobile": "CAPACITOR_PLATFORM=true npm run build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "cap:sync": "cap sync",
    "cap:sync:android": "cap sync android",
    "cap:open:android": "cap open android",
    "cap:run:android": "cap run android",
    "android:build": "npm run build:mobile && cap sync android",
    "android:open": "npm run android:build && cap open android",
    "android:dev": "npm run build:mobile && cap sync android && cap run android",
    "ios:build": "npm run build:mobile && cap sync ios",
    "ios:open": "npm run ios:build && cap open ios",
    "ios:dev": "npm run build:mobile && cap sync ios && cap run ios"
  },
  "devDependencies": {
    "@types/estree": "^1.0.8",
    "terser": "^5.44.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "vite": "^5.0.11"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.2",
    "@capacitor/camera": "^6.1.2",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1",
    "@capacitor/geolocation": "^6.1.0",
    "@capacitor/preferences": "^6.0.3",
    "@capacitor/push-notifications": "^6.0.4"
  }
}
